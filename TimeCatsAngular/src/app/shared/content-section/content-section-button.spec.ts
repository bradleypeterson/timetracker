import { ContentSectionButton } from './content-section-button';

describe('ContentSectionButton', () => {
  it('should create an instance', () => {
    expect(new ContentSectionButton()).toBeTruthy();
  });
});
